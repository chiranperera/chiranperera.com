# Public site URL (used for OG, sitemap, absolute redirects)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Database — set after `vercel link` + `vercel env pull`
DATABASE_URL=""

# Email (Resend) — Phase 3
RESEND_API_KEY=""
RESEND_FROM_EMAIL="noreply@chiranperera.com"

# Admin auth — Phase 3
# /admin is gated by HTTP Basic auth (single user). Pick a strong password.
ADMIN_USER="chiran"
ADMIN_PASSWORD=""               # set this before exposing /admin
ADMIN_EMAIL="chiran887@gmail.com"
AUTH_SECRET=""                  # reserved for a future magic-link upgrade

# LLM keys
GEMINI_API_KEY=""               # Phase 5a (provided by Chiran)
ANTHROPIC_API_KEY=""            # Phase 5b
OPENAI_API_KEY=""               # Phase 5b
PERPLEXITY_API_KEY=""           # Phase 5b

# Background jobs (QStash) — Phase 5
QSTASH_TOKEN=""
QSTASH_CURRENT_SIGNING_KEY=""
QSTASH_NEXT_SIGNING_KEY=""

# Spam protection (Cloudflare Turnstile) — Phase 3
TURNSTILE_SECRET_KEY=""
NEXT_PUBLIC_TURNSTILE_SITE_KEY=""
